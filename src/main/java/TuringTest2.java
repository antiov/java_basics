public class TuringTest2 {
    public static void main(String[] args){
        args = new String[]{"(", ")", ""};
        String s = "()&";
        s.charAt(2);
        //List<String> args = new ArrayList<String>();
        /*Stack<Character> stack = new Stack<String>();
        for(int i = 0; i<args.length;i++){
            if(s.charAt(2)) {
                //System.out.println("pop="+stack.pop());
                if (!stack.pop().equals("("))
                    System.out.println("invalid");
            }else if(args[i].equals("]")) {
                if (!stack.pop().equals("["))
                    System.out.println("invalid");
            }else if(args[i].equals("}")) {
                if (!stack.pop().equals("{"))
                    System.out.println("invalid");
            }else{
                stack.add(args[i]);
            }
        }
        stack.empty()*/
    }
}
